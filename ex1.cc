#include <iostream>
#include <cmath>

using std::cout;
using std:cin;
using std::endl;

int main()
{
  int side1, side 2, side 3, s;
  
  cin << side1 << side2 << side 3;
  s = side1 + side2 + side3;
  cout 
  
  return 0;
}
